//SPECIAL OPERATOR
#include<stdio.h>
int main()
{
	int X=50;
	printf("size of the variable x is %d", size of (x));
	printf("\n size of the integer data type is %d",size of (int));
	printf("\n size of the character data type is %d",size of (char));
	printf("\n size of the floating data type is %d\n",size of (float));
	printf("the address of x is %d \n",&x);
	return 0;
}
